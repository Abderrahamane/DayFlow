name: dayflow
description: A smart daily planner for managing tasks, notes, and reminders
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  fl_chart: ^0.66.0
  flutter_localizations:
    sdk: flutter
  intl: 0.20.2
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  country_flags: ^3.0.0
  url_launcher: ^6.2.0  # For email functionality

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  google_sign_in: ^6.2.1

  # Analytics
  mixpanel_flutter: ^2.3.1

  # State Management

  # UI Components
  cupertino_icons: ^1.0.6
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  flutter:
    sdk: flutter
  # ... your other packages like provider, sqflite, etc.

  # <<< --- ADD THIS NEW LINE --- >>>
  flutter_staggered_grid_view: ^0.7.0 # For the beautiful masonry grid layout


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true


  # Uncomment to add assets
  # assets:
  #   - images/
  #   - icons/

  # Uncomment to add custom fonts
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: fonts/Poppins-Regular.ttf
  #       - asset: fonts/Poppins-Bold.ttf
  #         weight: 700
